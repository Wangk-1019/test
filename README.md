# ChatGPT Clone - Grok Chat

基于 Next.js 和 Grok-4-Fast 模型的聊天应用，类似 ChatGPT 的界面设计。

## 功能特性

- 🎨 类似 ChatGPT 的两栏布局设计
- 💬 实时聊天对话
- 📝 聊天历史记录（本地存储）
- 🗑️ 删除对话功能
- 🚀 使用 Grok-4-Fast 模型
- 📱 响应式设计

## 技术栈

- **Next.js 14** - React 框架
- **TypeScript** - 类型安全
- **CSS Modules** - 样式管理
- **AI Builders API** - 后端 API（通过 MCP）

## 开始使用

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

确保 `.env.local` 文件包含以下配置：

```
AI_BUILDER_TOKEN=your_token_here
AI_BUILDER_BASE_URL=https://space.ai-builders.com/backend
```

### 3. 运行开发服务器

```bash
npm run dev
```

打开 [http://localhost:3000](http://localhost:3000) 查看应用。

## 项目结构

```
.
├── app/
│   ├── api/
│   │   └── chat/
│   │       └── route.ts      # API 路由处理聊天请求
│   ├── globals.css            # 全局样式
│   ├── layout.tsx             # 根布局
│   └── page.tsx               # 主页面
├── components/
│   ├── Sidebar.tsx            # 左侧聊天历史栏
│   ├── Sidebar.module.css
│   ├── ChatWindow.tsx         # 右侧聊天窗口
│   └── ChatWindow.module.css
├── .env.local                 # 环境变量配置
├── package.json
└── tsconfig.json
```

## API 说明

应用使用 AI Builders Space API，通过 `/api/chat` 路由调用 Grok-4-Fast 模型。

### API 端点

- **POST /api/chat**
  - 请求体: `{ messages: Array<{ role: 'user' | 'assistant', content: string }> }`
  - 响应: `{ content: string, model: string, usage: object }`

## 使用说明

1. **开始新对话**: 点击左侧栏的"新对话"按钮
2. **发送消息**: 在输入框中输入消息，按 Enter 发送（Shift+Enter 换行）
3. **查看历史**: 点击左侧栏中的对话记录
4. **删除对话**: 将鼠标悬停在对话项上，点击删除按钮

## 数据存储

聊天记录存储在浏览器的 `localStorage` 中，包括：
- 对话列表
- 每条对话的消息历史
- 当前选中的对话

## 开发

### 构建生产版本

```bash
npm run build
npm start
```

### 代码检查

```bash
npm run lint
```

## 注意事项

- 确保 MCP 服务器已正确配置
- API Token 需要有效且有足够的配额
- 聊天记录仅存储在本地浏览器中
