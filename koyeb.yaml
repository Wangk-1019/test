# Koyeb 部署配置
build:
  type: dockerfile
run:
  command: node server.js
  env:
    - name: PORT
      value: "3000"
    - name: NODE_ENV
      value: "production"
    - name: HOSTNAME
      value: "0.0.0.0"
